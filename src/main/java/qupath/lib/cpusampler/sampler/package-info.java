/**
 * This package contains the CPU sampler.
 * <ul>
 *     <li>
 *         The {@link qupath.lib.cpusampler.sampler.CPUSampler CPUSampler} class
 *         can repetitively sample the status of every thread in a background thread.
 *     </li>
 *     <li>
 *         The {@link qupath.lib.cpusampler.sampler.StackFrameNode StackFrameNode} class describes
 *         a stack frame element sampled by the CPU sampler.
 *     </li>
 * </ul>
 */
package qupath.lib.cpusampler.sampler;